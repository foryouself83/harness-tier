# Prose discipline here

`rules/` ships to consumers and nothing loads it in a session on this repo, so this file
imports the prose SSOT — every edit here carries prose. The checker is
`scripts/doc_style_check.py`; the rule names the path a host's copy sits at.

@../../../rules/doc-style.md
