"""Behavior spec for scripts/bump_version.py's `next` subcommand.

Covers the Release-Level trailer parser, the pending-rc/last-stable tag readers, and the
next_version arithmetic every release workflow's shared shell block calls.
"""
